name: ci-develop-pipeline
on:
  push:
    branches:
      - develop
jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v3
      - name: mostra a mensagem
        run: echo "O meu pipeline de desenvolvimento está funcionando" 